import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'featuredProducts',
  webDir: '../../dist/apps/featured-products',
  bundledWebRuntime: false,
};

export default config;
