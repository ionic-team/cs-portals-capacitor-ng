import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'host',
  webDir: '../../dist/apps/host',
  bundledWebRuntime: false,
};

export default config;
